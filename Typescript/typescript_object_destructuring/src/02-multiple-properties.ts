// Destructuring multiple properties

let product = {
    name: "Mobile",
    price: 25000,
    brand: "Samsung"
};

let { name, price, brand } = product;

console.log(name);
console.log(price);
console.log(brand);
