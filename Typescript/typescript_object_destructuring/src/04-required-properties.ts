// Extract only the required properties

let employee = {
    name: "Priya",
    age: 25,
    department: "IT",
    salary: 50000
};

let { name, department } = employee;

console.log(name);
console.log(department);
